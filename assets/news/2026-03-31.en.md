Just a few days ago, the legacy udp versionsrv has been deprecated.

Now, the client will get the new version info from [version.json](version.json).

Legacy versionsrv (version.teeworlds.wiki) would be kept, serving for non-Archive clients.